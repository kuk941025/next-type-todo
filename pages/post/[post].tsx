import { useRouter } from "next/router";

const Detail: React.FC = () => {
    const router = useRouter();
    console.log(router);
    return (
        <div>
            
        </div>
    )
}

export default Detail;